import MainContainer from "../src/components/MainContainer";

const Settings = () => {
    return (
        <MainContainer>
            <div>
                SETTINGS
            </div>
        </MainContainer>
    );
};

export default Settings;