import MainContainer from "../components/MainContainer";

const Settings = () => {
    return (
        <MainContainer>
            <div>
                SETTINGS
            </div>
        </MainContainer>
    );
};

export default Settings;